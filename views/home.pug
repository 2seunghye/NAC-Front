extends layouts/main
include mixins/videoBlock

block content
    .content__column
            each content in arrays
                h2= content.methodName 
                p= content.subject.name
                p= content.subject.macAddress
                p= content.objects[0].name
                p= content.objects[0].macAddress
                p= content.objects[1].name
                p= content.objects[1].macAddress
                p= content.scName
                p= content.abi
                br


    //- .home-videos
    //-     each item in videos 
    //-         +videoBlock({
    //-             id:item.id,
    //-             title:item.title,
    //-             views:item.views,
    //-             videoFile:item.fileUrl
    //-         })